name: Test App
on: 
  push:
    branches:
      - master
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Cypress run
        uses: actions/setup-node@v3
      - run: yarn install
      - run: yarn run build